module Polynomial.Test where

import Polynomial.Type
import Autolib.TES.Identifier
import Autolib.ToDoc

x = poly [(1, mono [(Factor (mk 0 "x") 1)]) ]
y = poly [(1, mono [(Factor (mk 0 "y") 1)]) ]
z = poly [(1, mono [(Factor (mk 0 "z") 1)]) ]

