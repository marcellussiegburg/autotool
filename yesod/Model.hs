module Model where

import Data.Int (Int)
import Data.Text (Text)

type AufgabeId = Int
type GruppeId = Int
type SchuleId = Int
type SemesterId = Int
type ServerUrl = Text
type AufgabeTyp = Text
type VorlesungId = Int
