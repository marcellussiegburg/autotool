module Model where

import Data.Int (Int)
import Data.Text (Text)
import Data.Tree (Tree)

import Yesod (Route)

type SchuleId = Int
type SemesterId = Int
type VorlesungId = Int
type GruppeId = Int
type AufgabeId = Int
