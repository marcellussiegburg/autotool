module Model where

import Data.Int (Int)
import Data.Text (Text)

type AufgabeId = Int
type AufgabeKonfiguration = Text
type AufgabeTyp = Text
type GruppeId = Int
type SchuleId = Int
type SemesterId = Int
type ServerUrl = Text
type VorlesungId = Int
